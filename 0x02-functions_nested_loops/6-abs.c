#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @j: number as an integer
 * Return: the absolute value
 */
int _abs(int j)
{
	if (j >= 0)
	{
		return (j);
	}
	else
	{
		return (j * -1);
	}
}
